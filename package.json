{
  "name": "projeto-sirene",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Projeto Sirene - drama de sobrevivencia civil em guerra moderna (prototipo)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gerar-arte": "node --env-file-if-exists=.env scripts/gerar-arte.mjs",
    "sync-sprites": "node scripts/gerar-arte.mjs --sync-only"
  },
  "dependencies": {
    "phaser": "^4.1.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "wrangler": "^4.99.0"
  }
}
