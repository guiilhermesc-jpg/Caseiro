{
  "name": "projeto-sirene",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Projeto Sirene - drama de sobrevivencia civil em guerra moderna (prototipo)",
  "scripts": {
    "dev": "vite",
    "dev:app": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "preview:app": "vite preview --host 0.0.0.0",
    "audit:package": "node scripts/auditar-pacote.mjs",
    "gerar-arte": "node --env-file-if-exists=.env scripts/gerar-arte.mjs",
    "sync-sprites": "node scripts/gerar-arte.mjs --sync-only"
  },
  "dependencies": {
    "phaser": "^4.1.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "wrangler": "^4.99.0"
  }
}
